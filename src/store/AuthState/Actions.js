// import * as Api from 'api/authAPI';
export const Actions = (set, get) => ({
  // login: async payload => {
  //   try {
  //     const { user, token, refreshToken } = await Api.login({ user: payload });
  //     set(() => ({ user, token, refreshToken }), true);
  //   } catch (err) {
  //     console.log(err);
  //   }
  // }, // by using set methos we can change the state
  // logout: () => set({ email: '', password: '', token: '' }),
});
